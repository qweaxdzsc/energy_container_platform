import argparse

# ============ parse =================
parser = argparse.ArgumentParser()
# parser.add_argument('-path', action='store', dest='project_address', help='define the project path')
# parser.add_argument('-cad_name', action='store', dest='project_address', help='define the project path')
# parser.add_argument('-json_path', action='store', dest='json_path', help='specify the json file path for parameters')
# parser.add_argument('-cad', action='store', dest='launch_CAD', help='define whether launch CAD(bool)')
# parser.add_argument('-solve', action='store', dest='launch_mesh_solver',
#                     help='define whether launch mesh and solver(bool)')
# parser.add_argument('-cores', action='store', dest='cores', help='define the threads to run task')
#
# outer_coefficients = parser.parse_args()
#
# json_path = outer_coefficients.json_path
# launch_CAD = outer_coefficients.launch_CAD
# launch_mesh_solver = outer_coefficients.launch_mesh_solver
# cores = outer_coefficients.cores
# print(outer_coefficients)
#
#
# parameter = dict()
